#' Write array commands to file
#'
#' This function writes generated commands to a file for SGE_Array submission
#' @param commands character; a vector of single-line commands to write to the file.
#' @param command.file character; path and name of text file to write commands for SGE_Array.
#' @seealso \code{\link{writeLines}}, \code{\link{generate.tool.command}}, \code{\link{generate.full.commands}}
#' @export

write.commands.file <- function(commands, command.file) {
  writeLines(text = commands, con = command.file)
}

